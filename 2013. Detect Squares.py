class Solution:
    
    
    def multiply_cheat_approach(self, num1: str, num2: str) -> str:
        return str(int(num1) * int(num2))
        # time: O(n + m), where n, m are length of num1, num2 respectively
        # space: O(1)
